import java.util.ArrayList;

public abstract class Zorde extends GameCharacter {

    // Constructor

    public Zorde(String ID, int HP, ArrayList<Integer> coordinates, int AP, int maximumMove) {
        super(ID, HP, coordinates, AP, maximumMove);
    }
}
